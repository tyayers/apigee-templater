<APIProxy revision="8" name="{{proxyName}}">
    <DisplayName>{{proxyName}}</DisplayName>
    <Description></Description>
    <CreatedAt>{{createdAt}}</CreatedAt>
    <LastModifiedAt>{{createdAt}}</LastModifiedAt>
    <BasePaths>{{basePath}}</BasePaths>
    <Policies>
      {{#each policies}}
        <Policy>{{this.name}}</Policy>
      {{/each}}
    </Policies>
    <ProxyEndpoints>
        <ProxyEndpoint>default</ProxyEndpoint>
    </ProxyEndpoints>
    <TargetEndpoints>
        <TargetEndpoint>default</TargetEndpoint>
    </TargetEndpoints>
</APIProxy>